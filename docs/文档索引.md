# 博客系统后端设计文档索引

> 文档集状态：面向当前代码实现与高级 Go 后端面试准备
>
> 最后核对：2026-09-01
>
> 事实来源：`meta-api` 当前代码；涉及前端时同时核对 `portal-web` 与 `admin-ui`

本文档集面向高级 Golang 后端开发工程师面试准备，目标不是简单罗列代码，而是把当前博客系统拆成可讲清楚的设计主题：业务建模、缓存、鉴权、风控、CDN、浏览量、JSON 分享、后端架构与未来演进。

## 文档列表

| 文档 | 说明 |
|---|---|
| [数据库设计](./数据库设计.md) | 从业务模型、表关系、索引、读写路径、一致性与后续扩展角度说明 MySQL 设计。 |
| [缓存设计](./缓存设计.md) | 说明 Redis 在文章列表、详情、浏览量、标签、友链、风控、限流中的职责，以及为什么采用主动失效和批量回写。 |
| [鉴权与风控系统设计](./鉴权与风控系统设计.md) | 说明管理员认证、普通用户 OAuth 评论身份、匿名行为风控 guard 三条链路的边界与安全策略。 |
| [后端代码架构](./后端代码架构.md) | 说明 Gin + GORM + Redis + dig DI 的分层结构、启动生命周期、路由组织、配置热更新和工程边界。 |
| [CDN 与边缘缓存设计](./内容分发网络与边缘缓存设计.md) | 区分代码内的文章 HTML Purge、仓库外 EdgeOne/Nginx 运维规则，以及尚未补齐的静态资源预热能力。 |
| [文章浏览量设计](./文章浏览量设计.md) | 说明前端行为采集、WASM 加密信封、后端风控评估、Redis 计数和 MySQL 持久化。 |
| [JSON 分享设计](./结构化数据分享设计.md) | 说明 JSON 分享的 precheck token、scope/target 绑定、Nuxt SSR 存储和安全边界。 |
| [评论审核系统设计与高级 Go 面试讲述](./评论审核系统设计.md) | 评论审核唯一专题文档，统一说明讲述主线、当前实现、工程取舍、审计反馈、性能问题、测试治理和常见追问。 |
| [未来优化与微服务演进](./未来优化与微服务演进.md) | 从单体项目到服务拆分、可观测性、灰度、高可用、数据治理角度规划后续方向。 |
| [高级 Golang 后端面试讲述提纲](./高级后端面试讲述提纲.md) | 将多份设计文档压缩成面试讲述顺序、技术亮点和常见追问回答。 |

## 当前系统边界

当前博客项目由四个主要工程组成：

| 工程 | 职责 |
|---|---|
| `meta-api` | Go 后端，负责文章、标签、评论、用户、站点动态、后台管理、浏览量、图片资产、CDN 清理、风控评估。 |
| `portal-web` | Nuxt SSR 前台，负责博客页面、文章详情、JSON 工具、浏览量打点、JSON 分享前端入口。 |
| `admin-ui` | 后台管理系统，负责文章管理、草稿管理、图片管理、评论和用户管理。 |
| `crypto-wasm` | Rust/WASM 风控核心，负责在浏览器侧打包行为数据、混合加密并生成 guard 信封；该工程不在当前 `blogWebsite` 同级目录中，文档涉及它时应注明实际仓库或发布产物位置。 |

## 文档状态约定

本文档集把内容分成三类，避免把设想误写成已经上线的能力：

| 状态 | 含义 |
|---|---|
| 当前实现 | 可以从当前代码、配置或路由直接验证。 |
| 运维约定 | 依赖 Nginx、CDN 控制台、部署参数等仓库外配置，代码本身不能完全证明。 |
| 条件式演进 | 尚未实现；只有触发条件成立并接受相应成本后才考虑。 |

如果文档与代码冲突，以代码为准；涉及生产安全约束时，还需要同时核对实际部署配置。

## 面试讲述主线

推荐按以下顺序准备：

1. 面试前先看 [高级 Golang 后端面试讲述提纲](./高级后端面试讲述提纲.md)，建立整体讲述主线。
2. 再讲后端整体架构：单体但按领域模块拆分，Handler、Service、Model 分层清晰，DI 统一装配。
3. 然后讲数据库设计：文章、草稿、标签、图片资产、评论、用户、管理员等核心表如何支撑业务。
4. 接着讲缓存设计：为什么文章列表和浏览量放 Redis，为什么浏览量异步回写 MySQL。
5. 再讲鉴权系统：管理员强认证、普通用户 OAuth、匿名行为 guard 三类安全边界不同。
6. 然后讲评论自动审核：为什么采用本地规则管线，如何降低误杀和支持人工复核。
7. 最后讲重点模块：CDN、浏览量、JSON 分享，以及未来从单体向可观测、高可用和微服务演进。

## 需要重点强调的设计取舍

| 设计点 | 取舍 |
|---|---|
| 文章和草稿同表 | 降低表数量和发布转换复杂度，通过 `status` 与 `published_id` 区分状态。 |
| 文章列表用 Redis ZSet | 列表排序和热门文章是高频读，ZSet 可直接按时间和浏览量分页。 |
| 浏览量先写 Redis | 避免每次访问直接更新 MySQL，降低写放大和行锁竞争。 |
| 图片资产引用增量同步 | 文章发布和更新时只同步单篇引用关系，不再做全量扫描。 |
| 评论审核本地规则管线 | 评论提交主链路不依赖外部审核 API，通过词库、结构、组合、行为和语义修正综合决策。 |
| JSON 分享 token 绑定 scope/target | 防止预检 token 被跨用途复用，避免“我的分享” token 被拿去创建分享。 |
| CDN Purge 放在 Go 后端 | 文章变更由源头服务同步清理边缘缓存，避免 Nuxt 和 CDN 状态割裂。 |
| guard 不是登录态 | guard 只证明请求具备一定浏览器行为特征，真正权限仍由业务鉴权决定。 |
